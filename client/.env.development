# Development Environment Variables
# Using PRODUCTION backend (Render) with updated CORS settings

# For PRODUCTION backend (Render)
REACT_APP_API_URL=https://restaurant-ycyh.onrender.com

# For LOCAL backend (uncomment if running server locally)
# REACT_APP_API_URL=http://localhost:3001
